{
  "name": "wedora-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.5"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": [
      "pnpm --filter frontend exec npm run lint --if-present"
    ],
    "backend/**/*.{js,ts}": [
      "pnpm --filter backend exec npm run lint --if-present"
    ]
  }
}
